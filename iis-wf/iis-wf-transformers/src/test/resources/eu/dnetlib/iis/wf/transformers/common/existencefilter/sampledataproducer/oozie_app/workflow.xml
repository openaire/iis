<workflow-app xmlns="uri:oozie:workflow:0.4" name="test-existencefilter_sampledataproducer">

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>mapred.job.queue.name</name>
                <value>${queueName}</value>
            </property>
            <property>
                <name>oozie.launcher.mapred.job.queue.name</name>
                <value>${oozieLauncherQueueName}</value>
            </property>
        </configuration>
    </global>
    
    
    <start to="producer"/>
    <action name="producer">
        <java>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${workingDir}/producer" />
				<mkdir path="${nameNode}${workingDir}/producer" />
			</prepare>
            <!-- This is simple wrapper for the Java code -->
			<main-class>eu.dnetlib.iis.common.java.ProcessWrapper</main-class>
			<!-- The business Java code that gets to be executed -->
			<arg>eu.dnetlib.iis.common.java.jsonworkflownodes.Producer</arg>
            <!-- Specification of the output ports -->
            <arg>-C{existent_id,
            eu.dnetlib.iis.common.schemas.DocumentId,
            eu/dnetlib/iis/wf/transformers/common/existencefilter/sampledataproducer/data/existent_id.json}</arg>
            <arg>-C{data,
            eu.dnetlib.iis.importer.auxiliary.schemas.DocumentContentUrl,
            eu/dnetlib/iis/wf/transformers/common/existencefilter/sampledataproducer/data/data.json}</arg>
            <!-- All input and output ports have to be bound to paths in HDFS -->
            <arg>-Oexistent_id=${workingDir}/producer/existent_id</arg>
            <arg>-Odata=${workingDir}/producer/data</arg>
        </java>
        <ok to="existencefilter"/>
        <error to="fail"/>
    </action>
    <action name="existencefilter">
        <sub-workflow>
            <app-path>${wf:appPath()}/existencefilter</app-path>
            <configuration>
                <property>
                    <name>jobTracker</name>
                    <value>${jobTracker}</value>
                </property>
                <property>
                    <name>nameNode</name>
                    <value>${nameNode}</value>
                </property>
                <property>
                    <name>queueName</name>
                    <value>${queueName}</value>
                </property>
                <property>
                    <name>oozieLauncherQueueName</name>
                    <value>${oozieLauncherQueueName}</value>
                </property>
                <!-- Input ports. -->
                <property>
                    <name>input_data</name>
                    <value>${workingDir}/producer/data</value>
                </property>
                <property>
                    <name>input_existent_id</name>
                    <value>${workingDir}/producer/existent_id</value>
                </property>
                <!-- Output port bound to given path -->
                <property>
                    <name>output_filtered</name>
                    <value>${workingDir}/existencefilter/filtered</value>
                </property>
            </configuration>
        </sub-workflow>
        <ok to="consumer"/>
        <error to="fail"/>
    </action>
    <action name="consumer">
		<java>
			<!-- This is simple wrapper for the Java code -->
			<main-class>eu.dnetlib.iis.common.java.ProcessWrapper</main-class>
			<!-- The business Java code that gets to be executed -->
			<arg>eu.dnetlib.iis.common.java.jsonworkflownodes.TestingConsumer</arg>
			<!-- Specification of the input ports -->
			<arg>-C{filtered,
                eu.dnetlib.iis.importer.auxiliary.schemas.DocumentContentUrl,
                eu/dnetlib/iis/wf/transformers/common/existencefilter/sampledataproducer/data/filtered.json}</arg>
    		<!-- All input and output ports have to be bound to paths in HDFS -->
			<arg>-Ifiltered=${workingDir}/existencefilter/filtered</arg>
		</java>
		<ok to="end" />
		<error to="fail" />
	</action>
    <kill name="fail">
		<message>Unfortunately, the workflow failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>
</workflow-app>