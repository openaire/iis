<workflow-app xmlns="uri:oozie:workflow:0.4" name="test-ingest_pmc_metadata_sampledataproducer">

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>mapred.job.queue.name</name>
                <value>${queueName}</value>
            </property>
            <property>
                <name>oozie.launcher.mapred.job.queue.name</name>
                <value>${oozieLauncherQueueName}</value>
            </property>
        </configuration>
    </global>
    
    
    <start to="producer"/>
    <action name="producer">
        <java>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${workingDir}/producer" />
				<mkdir path="${nameNode}${workingDir}/producer" />
			</prepare>
            <!-- This is simple wrapper for the Java code -->
			<main-class>eu.dnetlib.iis.common.java.ProcessWrapper</main-class>
			<!-- The business Java code that gets to be executed -->
			<arg>eu.dnetlib.iis.common.java.jsonworkflownodes.Producer</arg>
            <!-- Specification of the output ports -->
            <arg>-C{document_nlm,
            eu.dnetlib.iis.metadataextraction.schemas.DocumentText,
            eu/dnetlib/iis/wf/ingest/pmc/metadata/sampledataproducer/data/document_nlm.json}</arg>
            <arg>-Odocument_nlm=${workingDir}/producer/document_nlm</arg>
        </java>
        <ok to="ingest_pmc_metadata"/>
        <error to="fail"/>
    </action>
    
    <action name="ingest_pmc_metadata">
        <sub-workflow>
            <app-path>${wf:appPath()}/ingest_pmc_metadata</app-path>
            <configuration>
            	<property>
                    <name>jobTracker</name>
                    <value>${jobTracker}</value>
                </property>
                <property>
                    <name>nameNode</name>
                    <value>${nameNode}</value>
                </property>
                <property>
                    <name>queueName</name>
                    <value>${queueName}</value>
                </property>
                <property>
                    <name>oozieLauncherQueueName</name>
                    <value>${oozieLauncherQueueName}</value>
                </property>
                <property>
                    <name>workingDir</name>
                    <value>${workingDir}/ingest_pmc_metadata/working_dir</value>
                </property>
                <property>
                    <name>input</name>
                    <value>${workingDir}/producer/document_nlm</value>
                </property>
                <property>
                    <name>output_root</name>
                    <value>${workingDir}/ingest_pmc_metadata/out</value>
                </property>
            </configuration>
        </sub-workflow>
		<ok to="consumer"/>
        <error to="fail"/>
    </action>
    
    <action name="consumer">
		<java>
			<!-- This is simple wrapper for the Java code -->
			<main-class>eu.dnetlib.iis.common.java.ProcessWrapper</main-class>
			<!-- The business Java code that gets to be executed -->
			<arg>eu.dnetlib.iis.common.java.jsonworkflownodes.TestingConsumer</arg>
			<!-- Specification of the input ports -->
			<arg>-C{metadata,
                eu.dnetlib.iis.ingest.pmc.metadata.schemas.ExtractedDocumentMetadata,
                eu/dnetlib/iis/wf/ingest/pmc/metadata/sampledataproducer/data/metadata.json}</arg>
            <arg>-C{fault,
                eu.dnetlib.iis.audit.schemas.Fault,
                eu/dnetlib/iis/wf/ingest/pmc/metadata/sampledataproducer/data/fault.json}</arg>
    		<!-- All input and output ports have to be bound to paths in HDFS -->
			<arg>-Imetadata=${workingDir}/ingest_pmc_metadata/out/meta</arg>
			<arg>-Ifault=${workingDir}/ingest_pmc_metadata/out/fault</arg>
		</java>
		<ok to="end" />
		<error to="fail" />
	</action>
    <kill name="fail">
		<message>Unfortunately, the workflow failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>
</workflow-app>