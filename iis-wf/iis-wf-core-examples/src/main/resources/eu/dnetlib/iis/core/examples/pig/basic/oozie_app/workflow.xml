<workflow-app xmlns="uri:oozie:workflow:0.4" name="test-core_examples_pig_basic">

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>mapreduce.job.queuename</name>
                <value>${queueName}</value>
            </property>
            <property>
                <name>oozie.launcher.mapreduce.job.queuename</name>
                <value>${oozieLauncherQueueName}</value>
            </property>
        </configuration>
    </global>
    
    
    <start to="producer"/>
    <action name="producer">
        <java>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${workingDir}/producer" />
				<mkdir path="${nameNode}${workingDir}/producer" />
			</prepare>
            <main-class>eu.dnetlib.iis.common.java.ProcessWrapper</main-class>
            <arg>eu.dnetlib.iis.core.examples.java.TextDataProducer</arg>
            <!-- All input and output ports have to be bound to paths in 
            HDFS, working directory has to be specified as well -->
            <arg>-Operson=${workingDir}/producer/person.csv</arg>
            <arg>-Odocument=${workingDir}/producer/doc.csv</arg>
        </java>
        <ok to="pig_node"/>
        <error to="fail"/>
    </action>
    <action name="pig_node">
        <pig>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${workingDir}/pig_node" />
				<mkdir path="${nameNode}${workingDir}/pig_node" />
			</prepare>
            <configuration>
                <property><!-- TODO: What is this? Delete it? -->
                    <name>mapreduce.map.output.compress</name>
                    <value>true</value>
                </property>
            </configuration>
            <script>script.pig</script>
            <param>person=${workingDir}/producer/person.csv</param>
            <param>person_id=${workingDir}/pig_node/person_id</param>
        </pig>
        <ok to="end"/>
        <error to="fail"/>
    </action>
    <kill name="fail">
		<message>Unfortunately, the workflow failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>
</workflow-app>
