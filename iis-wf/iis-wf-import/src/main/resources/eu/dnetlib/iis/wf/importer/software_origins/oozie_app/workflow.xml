<workflow-app xmlns="uri:oozie:workflow:0.4" name="importer_software_origins">
	
	<parameters>
		<property>
			<name>input_csv</name>
			<description>input CSV file location with software heritage origin and url pairs</description>
		</property>
		<property>
			<name>output</name>
			<description>software heritage origins avro datastore output</description>
		</property>
        <property>
            <name>output_report_root_path</name>
            <description>base directory for storing reports</description>
        </property>
        <property>
            <name>output_report_relative_path</name>
            <value>import_software_origins</value>
            <description>directory for storing report (relative to output_report_root_path)</description>
        </property>
        <property>
            <name>sparkDriverMemory</name>
            <description>memory for driver process</description>
        </property>
        <property>
            <name>sparkExecutorMemory</name>
            <description>memory for individual executor</description>
        </property>
        <property>
            <name>sparkExecutorCores</name>
            <description>number of cores used by single executor</description>
        </property>
	</parameters>

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>mapreduce.job.queuename</name>
                <value>${queueName}</value>
            </property>
            <property>
                <name>oozie.launcher.mapred.job.queue.name</name>
                <value>${oozieLauncherQueueName}</value>
            </property>
        </configuration>
    </global>
    
    
	<start to="softwareheritage-importer" />
	
	<action name="softwareheritage-importer">
		<spark xmlns="uri:oozie:spark-action:0.2">

            <!-- The data generated by this node is deleted in this section -->
            <prepare>
                <delete path="${nameNode}${output}" />
                <delete path="${nameNode}${output_report_root_path}/${output_report_relative_path}" />
            </prepare>

            <master>yarn-cluster</master>
            <mode>cluster</mode>
            <name>import-softwareheritage-origins</name>
            <class>eu.dnetlib.iis.wf.importer.csv.SoftwareHeritageOriginsReaderJob</class>
            <jar>${oozieTopWfApplicationPath}/lib/iis-wf-import-${projectVersion}.jar</jar>
            <!-- spark.dynamicAllocation.enabled=true is already set in cluster configuration -->
            <spark-opts>--executor-memory ${sparkExecutorMemory} --executor-cores ${sparkExecutorCores} --driver-memory=${sparkDriverMemory}</spark-opts>
            
            <arg>-inputCSVLocation=${input_csv}</arg>
            <arg>-outputPath=${output}</arg>
            <arg>-outputReportPath=${output_report_root_path}/${output_report_relative_path}</arg>
        </spark>
		<ok to="end" />
		<error to="fail" />
	</action>

    
	<kill name="fail">
		<message>Unfortunately, the process failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
	</kill>
	<end name="end" />
</workflow-app>
