<workflow-app xmlns="uri:oozie:workflow:0.4" name="transformers_referenceextraction_project_filter">

	<parameters>
		<property>
			<name>input</name>
		</property>
		<property>
			<name>fundingclass_blacklist_regex</name>
		</property>
		<property>
			<name>output_approved</name>
		</property>
		<property>
			<name>output_rejected</name>
		</property>
	</parameters>

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>mapreduce.job.queuename</name>
                <value>${queueName}</value>
            </property>
            <property>
                <name>oozie.launcher.mapred.job.queue.name</name>
                <value>${oozieLauncherQueueName}</value>
            </property>
        </configuration>
    </global>
    
    
	<start to="generate-schema" />

	<action name="generate-schema">
		<java>
			<main-class>eu.dnetlib.iis.common.javamapreduce.hack.AvroSchemaGenerator</main-class>
			<arg>eu.dnetlib.iis.importer.schemas.Project</arg>
			<capture-output />
		</java>
		<ok to="transformer" />
		<error to="fail" />
	</action>

	<action name="transformer">
		<pig>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${output_approved}" />
				<delete path="${nameNode}${output_rejected}" />
			</prepare>
			<!-- Path to PIG script the workflow executes. -->
			<script>lib/scripts/transformer.pig</script>

			<param>input=${input}</param>
			<param>output_approved=${output_approved}</param>
			<param>output_rejected=${output_rejected}</param>
			<param>schema=${wf:actionData('generate-schema')['eu.dnetlib.iis.importer.schemas.Project']}</param>
			<param>fundingclass_blacklist_regex=${fundingclass_blacklist_regex}</param>
		</pig>
		<ok to="end" />
		<error to="fail" />
	</action>
	<kill name="fail">
		<message>Unfortunately, the workflow failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]
		</message>
	</kill>
	<end name="end" />
</workflow-app>
