<workflow-app xmlns="uri:oozie:workflow:0.4" name="export_actionmanager_xslt_test">

	<parameters>
		<property>
			<name>transformer_factory</name>
			<value>$UNDEFINED$</value>
		</property>
	</parameters>

	<start to="xslt_transformation" />

	<action name="xslt_transformation">
        <java>
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
			<prepare>
			</prepare>
            <configuration>
                <property>
                    <name>mapred.job.queue.name</name>
                    <value>${queueName}</value>
                </property>
            </configuration>
			<main-class>eu.dnetlib.iis.common.java.ProcessWrapper</main-class>
			<arg>eu.dnetlib.iis.workflows.export.actionmanager.entity.dataset.XsltTransformationTestProcess</arg>
            <arg>-Ptransformer_factory=${transformer_factory}</arg>
        </java>
        <ok to="end"/>
        <error to="fail"/>
    </action>
	
	<kill name="fail">
		<message>Unfortunately, the process failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
	</kill>
	<end name="end" />
</workflow-app>
