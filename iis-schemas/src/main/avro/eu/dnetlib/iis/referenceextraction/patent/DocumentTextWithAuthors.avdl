@namespace("eu.dnetlib.iis.referenceextraction.patent.schemas")
protocol IIS{

    record DocumentTextWithAuthors {
//      document identifier
//      foreign key: DocumentMetadata.id ("raw document metadata" data store)
        string id;
//      document fulltext
        string text;
//      document authors' fullnames
        array<string> authors;
    }
}
