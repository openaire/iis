@namespace("eu.dnetlib.iis.referenceextraction.project.schemas")
protocol IIS{

    record DocumentHash {
//      document hash value calculated as murmur3 hash of document title, abstract and text
        int hashValue;
    }
}