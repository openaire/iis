@namespace("eu.dnetlib.iis.importer.schemas")
protocol IIS{

    record Param {
        string name;
        string value;
    }

    record Concept {
        string id;
        string label;
        array<Param> params;
    }
}
