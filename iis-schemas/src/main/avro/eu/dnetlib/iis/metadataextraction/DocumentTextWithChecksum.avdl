@namespace("eu.dnetlib.iis.metadataextraction.schemas")
protocol IIS{

    record DocumentTextWithChecksum {
//      document identifier
//      foreign key: DocumentMetadata.id ("raw document metadata" data store)
        string id;
//      checksum of the source document
        string checksum;
//      document fulltext
        string text;
    }
}
