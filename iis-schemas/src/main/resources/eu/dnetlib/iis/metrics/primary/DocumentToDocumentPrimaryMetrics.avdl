@namespace("eu.dnetlib.iis.metrics.primary.schemas")
protocol IIS{

    import idl "DocumentPrimaryMetrics.avdl";
    
    record DocumentToDocumentPrimaryMetrics {
//      ID of the document, foreign key: Document.id ("document" data store)
        string documentId;
        union { null , DocumentPrimaryMetrics } primaryMetrics = null;
    }
}
