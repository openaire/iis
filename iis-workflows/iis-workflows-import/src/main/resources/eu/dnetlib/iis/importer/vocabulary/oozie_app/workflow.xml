<workflow-app xmlns="uri:oozie:workflow:0.4" name="importer_vocabulary">
	
	<parameters>
		<property>
			<name>islookup_service_location</name>
			<description>islookup service location</description>
		</property>
		<property>
			<name>output</name>
			<description>vocabulary output directory containing properties file</description>
		</property>
		<property>
			<name>output_filename</name>
			<description>vocabulary output properties file</description>
		</property>
	</parameters>

	<start to="vocabulary-importer" />
	
	<action name="vocabulary-importer">
		<java>
			<job-tracker>${jobTracker}</job-tracker>
			<name-node>${nameNode}</name-node>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${output}" />
			</prepare>
			<configuration>
				<property>
					<name>mapred.job.queue.name</name>
					<value>${queueName}</value>
				</property>
			</configuration>
			<main-class>eu.dnetlib.iis.core.java.ProcessWrapper</main-class>
			<arg>eu.dnetlib.iis.importer.vocabulary.ISLookupServiceBasedVocabularyImporter</arg>
			<arg>-Pimport.islookup.service.location=${islookup_service_location}</arg>
			<arg>-Pimport.vocabulary.output.filename=${output_filename}</arg>
			<arg>-Ovocabulary=${output}</arg>
		</java>
		<ok to="end" />
		<error to="fail" />
	</action>
	
	<kill name="fail">
		<message>Unfortunately, the process failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
	</kill>
	<end name="end" />
</workflow-app>
