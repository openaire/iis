<workflow-app xmlns="uri:oozie:workflow:0.3" name="test-importer_hbase_test">
	<!-- 
		This example writes to multiple datastores: 2 sequence files with persons of odd and even age. 
		Then another MR job reads both datastores and clones all persons.
	-->
	<start to="hbase_test" />
	<action name="hbase_test">
		<java>
			<job-tracker>${jobTracker}</job-tracker>
			<name-node>${nameNode}</name-node>
			<prepare>
			</prepare>
			<configuration>
				<property>
					<name>mapred.job.queue.name</name>
					<value>${queueName}</value>
				</property>
			</configuration>
			<main-class>eu.dnetlib.iis.core.java.ProcessWrapper</main-class>
			<arg>eu.dnetlib.iis.importer.HBaseStdoutPrinter</arg>
			<arg>-Pimport.hbase.table.name=mhorst_test</arg>
		</java>
		<ok to="end" />
		<error to="fail" />
	</action>
	
	<kill name="fail">
		<message>Unfortunately, the process failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
	</kill>
	<end name="end" />
</workflow-app>