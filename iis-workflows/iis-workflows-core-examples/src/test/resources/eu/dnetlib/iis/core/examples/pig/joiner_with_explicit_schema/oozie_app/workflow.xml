<workflow-app xmlns="uri:oozie:workflow:0.2" name="test-core_examples_pig_joiner_with_explicit_schema">
    <start to="producer"/>
    <action name="producer">
        <java>
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${workingDir}/producer" />
				<mkdir path="${nameNode}${workingDir}/producer" />
			</prepare>
            <configuration>
                <property>
                    <name>mapred.job.queue.name</name>
                    <value>${queueName}</value>
                </property>
            </configuration>
            <main-class>eu.dnetlib.iis.common.java.ProcessWrapper</main-class>
            <arg>eu.dnetlib.iis.core.examples.java.SampleDataProducer</arg>
            <!-- All input and output ports have to be bound to paths in 
            HDFS, working directory has to be specified as well -->
            <arg>-Operson=${workingDir}/producer/person</arg>
            <arg>-Odocument=${workingDir}/producer/doc</arg>
        </java>
        <ok to="joiner"/>
        <error to="fail"/>
    </action>
    <action name="joiner">
        <pig>
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${workingDir}/joiner" />
				<mkdir path="${nameNode}${workingDir}/joiner" />
			</prepare>
            <configuration>
                <property>
                    <name>mapred.job.queue.name</name>
                    <value>${queueName}</value>
                </property>
            </configuration>
            <!-- Path to PIG script the workflow executes. -->
            <script>lib/scripts/joiner/joiner.pig</script>
            <!-- Input ports. -->
            <param>input_person=${workingDir}/producer/person</param>
            <param>input_document=${workingDir}/producer/doc</param>
            <!-- Output ports. -->
            <param>output_document_with_authors=${workingDir}/joiner/document_with_authors</param>
            <param>output_person_with_documents=${workingDir}/joiner/person_with_documents</param>
            <param>output_person_age=${workingDir}/joiner/person_age</param>
            <!-- Input and output Avro schemas passed to the script must be given
                 as full URIs. -->
            <param>schema_input_document=${wf:appPath()}/lib/scripts/joiner/document.avsc</param>
            <param>schema_input_person=${wf:appPath()}/lib/scripts/joiner/person.avsc</param>
            <param>schema_output_document_with_authors=${wf:appPath()}/lib/scripts/joiner/document_with_authors.avsc</param>
            <param>schema_output_person_with_documents=${wf:appPath()}/lib/scripts/joiner/person_with_documents.avsc</param>
            <param>schema_output_person_age=${wf:appPath()}/lib/scripts/joiner/person_age.avsc</param>
        </pig>
        <ok to="end"/>
        <error to="fail"/>
    </action>
    <kill name="fail">
		<message>Unfortunately, the workflow failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>
</workflow-app>
