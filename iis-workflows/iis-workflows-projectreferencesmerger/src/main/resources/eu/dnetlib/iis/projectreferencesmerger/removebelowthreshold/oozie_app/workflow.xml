<workflow-app xmlns="uri:oozie:workflow:0.4" name="removebelowthreshold">

<!-- mh: code review note

removebelowthreshold workflow is dedicated solely to filtering project reference extraction output by confidenceLevel but it could be replaced with generic thresholdfilter addressed in #1210 and available here: 

https://svn.driver.research-infrastructures.eu/driver/dnet40/modules/icm-iis-transformers/trunk/src/main/resources/eu/dnetlib/iis/transformers/common/thresholdfilter

-->



	<parameters>
        <!-- Input datastores: -->
        <property>
			<name>input_from_project_reference_extraction</name>
			<description>records from the Project Reference Extraction module</description>
		</property>
        <!-- Input parameters: -->
		<property>
			<name>threshold</name>
			<description>records will be filtered by this threshold</description>
		</property>
        <!-- Output datastores: -->
        <property>
			<name>output</name>
			<description>input_from_project_reference_extraction records with confidenceLevel above the threshold</description>
		</property>
	</parameters>

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>mapred.job.queue.name</name>
                <value>${queueName}</value>
            </property>
        </configuration>
    </global>

    <start to="removebelowthreshold"/>
    <action name="removebelowthreshold">
        <pig>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${workingDir}/removebelowthreshold" />
				<delete path="${nameNode}${output}" />
				<mkdir path="${nameNode}${workingDir}/removebelowthreshold" />
			</prepare>

            <!-- Path to PIG script the workflow executes. -->
            <script>lib/scripts/removebelowthreshold.pig</script>
            <!-- The working directory of the workflow node. -->
            <param>workingDir=${workingDir}/removebelowthreshold/working_dir</param>

            <!-- Input datastores: -->
            <param>input_from_project_reference_extraction=${input_from_project_reference_extraction}</param>
            <param>schema_input_from_project_reference_extraction=eu.dnetlib.iis.referenceextraction.project.schemas.DocumentToProject</param>
            <!-- Input parameters: -->
            <param>threshold=${threshold}</param>
            <!-- Output datastores: -->
            <param>output=${output}</param>
            <param>schema_output=eu.dnetlib.iis.importer.schemas.DocumentToProject</param>
        </pig>
        <ok to="end"/>
        <error to="fail"/>
    </action>
    <kill name="fail">
		<message>Unfortunately, the workflow failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>
</workflow-app>
