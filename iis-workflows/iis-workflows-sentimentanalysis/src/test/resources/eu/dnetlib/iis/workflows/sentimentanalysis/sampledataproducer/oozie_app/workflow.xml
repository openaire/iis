<workflow-app xmlns="uri:oozie:workflow:0.3" name="test-sentimentanalyis_sampledataproducer">
    <start to="producer"/>
    <action name="producer">
        <java>
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${workingDir}/producer" />
				<mkdir path="${nameNode}${workingDir}/producer" />
			</prepare>
            <configuration>
                <property>
                    <name>mapred.job.queue.name</name>
                    <value>${queueName}</value>
                </property>
            </configuration>
            <!-- This is simple wrapper for the Java code -->
			<main-class>eu.dnetlib.iis.core.java.ProcessWrapper</main-class>
			<!-- The business Java code that gets to be executed -->
			<arg>eu.dnetlib.iis.core.java.jsonworkflownodes.Producer</arg>
            <!-- Specification of the output ports -->
            <arg>-C{input,
            eu.dnetlib.iis.sentimentanalysis.schemas.DocumentTextWithReferences,
            eu/dnetlib/iis/workflows/sentimentanalysis/sampledataproducer/data/document_text_with_references.json}</arg>
            <arg>-Oinput=${workingDir}/producer/input</arg>
        </java>
        <ok to="sentiment_analysis"/>
        <error to="fail"/>
    </action>
    
    <action name="sentiment_analysis">
        <sub-workflow>
            <app-path>${wf:appPath()}/sentiment_analysis</app-path>
            <configuration>
            	<property>
                    <name>jobTracker</name>
                    <value>${jobTracker}</value>
                </property>
                <property>
                    <name>nameNode</name>
                    <value>${nameNode}</value>
                </property>
                <property>
                    <name>queueName</name>
                    <value>${queueName}</value>
                </property>
                <property>
                    <name>workingDir</name>
                    <value>${workingDir}/sentiment_analysis/working_dir</value>
                </property>
                <property>
                    <name>input</name>
                    <value>${workingDir}/producer/input</value>
                </property>
                <property>
                    <name>output</name>
                    <value>${workingDir}/sentiment_analysis/out</value>
                </property>
            </configuration>
        </sub-workflow>
		<ok to="consumer"/>
        <error to="fail"/>
    </action>
    
    <action name="consumer">
		<java>
			<job-tracker>${jobTracker}</job-tracker>
			<name-node>${nameNode}</name-node>
			<configuration>
				<property>
					<name>mapred.job.queue.name</name>
					<value>${queueName}</value>
				</property>
			</configuration>
			<!-- This is simple wrapper for the Java code -->
			<main-class>eu.dnetlib.iis.core.java.ProcessWrapper</main-class>
			<!-- The business Java code that gets to be executed -->
			<arg>eu.dnetlib.iis.core.java.jsonworkflownodes.TestingConsumer</arg>
			<!-- Specification of the input ports -->
			<arg>-C{output,
                eu.dnetlib.iis.sentimentanalysis.schemas.ReferenceWithSentimentLabels,
                eu/dnetlib/iis/workflows/sentimentanalysis/sampledataproducer/data/sentiment_labels.json}</arg>
    		<!-- All input and output ports have to be bound to paths in HDFS -->
			<arg>-Ioutput=${workingDir}/sentiment_analysis/out</arg>
		</java>
		<ok to="end" />
		<error to="fail" />
	</action>
    <kill name="fail">
		<message>Unfortunately, the workflow failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>
</workflow-app>