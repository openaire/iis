<workflow-app xmlns="uri:oozie:workflow:0.4" name="transformers_documentsclassification">
	
	<parameters>
		<property>
			<name>input_merged_metadata</name>
			<description>input merged metadata directory</description>
		</property>
		<property>
			<name>output_document_metadata</name>
			<description>output directory</description>
		</property>
	</parameters>
    
    <start to="generate-schema"/>
    
    <action name="generate-schema">
	    <java>
    		<job-tracker>${jobTracker}</job-tracker>
        	<name-node>${nameNode}</name-node>
	        <main-class>eu.dnetlib.iis.core.javamapreduce.hack.AvroSchemaGenerator</main-class>
	        <arg>eu.dnetlib.iis.transformers.metadatamerger.schemas.ExtractedDocumentMetadataMergedWithOriginal</arg>
	        <arg>eu.dnetlib.iis.documentsclassification.schemas.DocumentMetadata</arg>
	        <capture-output />
	    </java>
	    <ok to="transformer" />
	    <error to="fail" />
	</action>
    
    <action name="transformer">
        <pig>
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
			<!-- The data generated by this node is deleted in this section -->
			<prepare>
				<delete path="${nameNode}${output_document_metadata}" />
			</prepare>
            <configuration>
                <property>
                    <name>mapred.job.queue.name</name>
                    <value>${queueName}</value>
                </property>
            </configuration>
            <!-- Path to PIG script the workflow executes. -->
            <script>lib/scripts/transformer.pig</script>

            <param>input_merged_metadata=${input_merged_metadata}</param>
            <param>schema_input_merged_metadata=${wf:actionData('generate-schema')['eu.dnetlib.iis.transformers.metadatamerger.schemas.ExtractedDocumentMetadataMergedWithOriginal']}</param>
           
            <param>output_document_metadata=${output_document_metadata}</param>
            <param>schema_output_document_metadata=${wf:actionData('generate-schema')['eu.dnetlib.iis.documentsclassification.schemas.DocumentMetadata']}</param>
        </pig>
        <ok to="end"/>
        <error to="fail"/>
    </action>
    <kill name="fail">
		<message>Unfortunately, the workflow failed -- error message:
			[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>
</workflow-app>
